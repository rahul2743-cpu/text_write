print("=" * 100)
print("1. DEBUG idf()")
print("=" * 100)

test_tokens = [
    "service",
    "operation",
    "rolls",
    "royce",
    "product",
    "engine"
]

for token in test_tokens:

    document_frequency = KG_DF.get(token, 0)
    token_idf = idf(token)

    print(
        f"Token: {token}\n"
        f"Document frequency: {document_frequency}\n"
        f"IDF score: {token_idf:.6f}\n"
    )
